# Docs

Tracked documentation should live here once the implementation stabilizes enough to warrant
developer-facing docs in version control.

Early design work continues under `spec/`, which is intentionally ignored by git.
