# Tests

This directory is reserved for integration and fault-injection tests.

The first high-value scenarios should cover:

- workflow replay after worker loss
- service turn recovery
- durable signal delivery
- subprocess timeout and artifact capture
